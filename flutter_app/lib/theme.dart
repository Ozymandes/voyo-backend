import 'package:flutter/material.dart';

class VoyoColors {
  // Backgrounds
  static const page = Color(0xFFF7F5F1);
  static const paper = Color(0xFFFFFFFF);
  static const vellum = Color(0xFFF0EBE3);
  static const smoke = Color(0xFFE8E2D8);

  // Brand accents
  static const expedition = Color(0xFFD45028);
  static const terra = Color(0xFFC4622A);
  static const sky = Color(0xFF1C72B4);
  static const discovery = Color(0xFF8860D4);
  static const discoveryAccessible = Color(0xFF6040B0);

  // Semantic
  static const verified = Color(0xFF2A7A50);
  static const caution = Color(0xFFD48A10);

  // Text
  static const ink = Color(0xFF1A1714);
  static const stone = Color(0xFF6A6058);
}
