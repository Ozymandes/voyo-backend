# Ch3 Methodology — FIGURES & TABLES SPEC

> **Purpose:** which figures/tables Ch3 needs, the exact data-source file for each, and the
> status (retained / regenerate / pending). Every figure that depends on the eval harness or
> a count is marked PENDING with the reason; no figure is fabricated.

---

## Figure inventory for Ch3

### Figure 3.1 — System Architecture (the four-layer hybrid)
- **Status:** ✅ **RETAINED** (per criteria §5 — `fig_3_1_architecture` is in the "retained,
  not count-dependent" list).
- **File:** `thesis/figures/fig_3_1_architecture.png` (already generated; do not regenerate).
- **Content (per §3.1):** the four layers — Presentation (Flutter, `flutter_map`) → Gateway
  (FastAPI, Supabase Auth) → Agentic Orchestration (CLEO ReAct + Recommendation engine +
  Itinerary curate→optimize) → Ground-Truth Data (Supabase `pois` table, 310 POIs) — with the
  deterministic engines (Valhalla, VROOM, OSRM) shown as the third-layer services CLEO
  dispatches to. The **curate→optimize split** should be visually emphasized: CLEO (left) →
  dispatch → engines (right), with the trust boundary as a visible line.
- **Citations the figure embeds:** [02 → Q1,Q2] (blueprint); [03 → Q3] (multi-agent
  substrate); [N1 → Q3] (curate→optimize motivation).
- **Notes for the writer:** if the figure needs a refresh to add the new §3.5 ablation
  Configuration-B callout (showing where the engines-bypassed path would inject), ask the
  figures agent to amend — but the canonical architecture figure is RETAINED as-is.

### Figures 3.2–3.4 — UI mockups (NOT fabricatable)
- **Status:** ⚠️ **FLAG — user must capture from the running app.** These are UI mockups
  (chat screen, map explorer with isochrone overlay, itinerary timeline), not data-driven
  figures. The dossier does NOT depend on them.
- **Action:** none from the dossier; the writer should capture live screenshots at defense
  time (the app must be running on a non-free-tier LLM provider for live CLEO — see §3.2.4).

---

## Figure inventory for Ch3 → cross-references to other chapters

### Figure 4.12 — Ablation comparison (the §3.5 keystone chart)
- **Status:** ⏸ **PENDING the eval harness** (criteria §5: "the single most defensible chart").
  Owned by Ch4 but specified in §3.5.7 here.
- **File (target):** `thesis/figures/fig_4_12_ablation.png` (TBD; will be generated by
  `thesis/figures/gen_all_figures.py` once the eval harness runs the two configurations).
- **Content (per §3.5.7):** grouped bars — X-axis: {feasibility %, constraint-violation rate,
  geographic coherence (Avg Margin)}; grouped by configuration: full-hybrid (Config A) vs
  LLM-only (Config B). Three metric groups × two bars each.
- **Threshold lines (overlay):**
  - feasibility: ≥ 90% (hybrid target) and ≤ 50% (LLM-only collapse target).
  - violation rate: < 5% (hybrid target).
  - Avg Margin: 86.0 (ItiNera full-system reference) and 242.8 (ItiNera w/o CSO reference) as
    horizontal comparison lines, so VOYO's numbers are visually anchored to the published
    peer-reviewed baseline.
- **Data source (when available):** produced by the eval harness — Config A vs Config B over
  identical scenarios; one row per scenario × configuration × metric. The harness should emit
  `thesis/evidence/08-ablation-results.json` (TBD; not yet present).
- **Source for the threshold comparands (available now):** [N1 → Q5, arXiv:2402.07204 Table 2
  p.6] (Avg Margin 86.0 / 242.8 verbatim).
- **Notes for the writer:** the chart is BLOCKING per criteria §5; do not let the eval harness
  ship without it. The pre-registered thresholds in §3.5.5 are what the chart tests against.

---

## Table inventory for Ch3

### Table 3.1 — The Trust Boundary Table (the excellence deliverable)
- **Status:** ✅ **DELIVERED in this dossier** (criteria §4 Ch3 Excellence bar: "a 'trust
  boundary' table (LLM does X / engines do Y)").
- **File:** reproduced verbatim in `dossier.md` §3.6.1 and `evidence-packet.md` §F.
- **Content:** 12 rows × {Class of computation, LLM (CLEO) does, Deterministic engines do,
  Load-bearing citation}. The ✅/❌/⚠️/FORBIDDEN markers make the boundary visually auditable.
- **Data source:** synthesized from `07-codebase-facts.md` (codebase facts) + the citation
  quote banks (Tier A/B/C). No number invented.
- **Notes for the writer:** this is the single most important artifact of Ch3; the supervisor's
  excellence bar is met by this table alone.

### Table 3.2 — Deterministic-substrate latency (the measured-now evidence)
- **Status:** ✅ **DELIVERED in `evidence-packet.md` §E** (reproduced from
  `thesis/evidence/02-latency.json`).
- **Content:** 13 benchmarks × {target, median, p95, status}; every row PASS. Headline:
  scoring 200 POIs p95 = 1.662 ms (≈300× under the 500 ms threshold).
- **Data source:** `thesis/evidence/02-latency.json` (re-run 2026-06-15; synthetic data; 3
  runs each, median = min of 3, p95 = max of 3).
- **Notes for the writer:** this table supports §3.4.6 (the substrate is effectively free); it
  reinforces the hybrid argument by showing the deterministic side does not consume the latency
  budget.

### Table 3.3 — CLEO force-tool policy (the grounding-fix verification)
- **Status:** ✅ **DELIVERED in `evidence-packet.md` §D.4** (reproduced from the 2026-06-14
  devlog via `06-cleo-grounding.md`).
- **Content:** 6 rows × {input, style, force_tool, result}; 6/6 PASS offline (no LLM, no
  quota).
- **Data source:** `thesis/evidence/06-cleo-grounding.md` §(B); cross-confirmed by the live
  trace (Iter 1 = `search_pois` with real DB data, Iter 2 = `curate_itinerary` with real IDs,
  Iter 3 = 429 quota).
- **Notes for the writer:** supports §3.2.2 (the force-tool grounding fix).

---

## Regenerate / refresh flags (criteria §5 stale-number sweep)

- ⚠️ **`thesis/evidence/05-db-completeness.json`** — currently reports `total_active_pois: 255`
  (STALE per criteria §4). The canonical POI count is **310**. This dossier uses 310
  throughout; the underlying JSON must be regenerated by `validate_database.py` against the
  live `pois` table before Ch6 closes. **Do not introduce 255 anywhere in Ch3 output.**
- ⚠️ **`thesis/evidence/06-cleo-grounding.md`** and **`thesis/evidence/07-codebase-facts.md`**
  and **`thesis/evidence/_GROUNDING_MAP.md`** — all reference the stale "255 active POIs" /
  "255-POI verified DB" phrasing. These are *evidence files* (read-only reference for the
  dossier writer); they are flagged for refresh but Ch3 output uses 310 per the criteria
  contract. See `dossier.md` "Open questions / escalations" Q-ESCALATE-1.
- ⚠️ **`thesis/figures/fig_field_completeness`** and **`fig_regional_distribution`** — were
  deleted per criteria §5 (they were 255-POI figures). They are NOT Ch3 figures (they belong
  to Ch6); flag for Ch6 regeneration from current data. Ch3 does not depend on them.

---

## Pending-metric summary for Ch3 (criteria §5 honesty rule)

| §3 metric | Threshold | Status | Blocker |
|---|---|---|---|
| Deterministic-substrate latency (§3.4.6) | p95 < 500 ms | ✅ **MEASURED NOW** — p95 1.662 ms | none |
| CLEO force-tool grounding logic (§3.2.2) | 8/8 offline pass | ✅ **MEASURED NOW** — 8/8 pass | none |
| VROOM optimize availability (§3.4.5) | n/a (status) | ⚠️ **intermittent/pending** | VROOM live responses; disclose at defense |
| Ablation Config A feasibility (§3.5.4) | ≥ 90% | ⏸ **PENDING** | eval harness |
| Ablation Config B feasibility (§3.5.4) | ≤ 50% target | ⏸ **PENDING** | eval harness |
| Ablation violation rate (§3.5.4) | < 5% | ⏸ **PENDING** | eval harness |
| Ablation Avg Margin (§3.5.4) | comparable to 86.0 (hybrid) / 242.8 (LLM-only) | ⏸ **PENDING** | eval harness |

**Rule (criteria §7):** no number is invented for any PENDING row. The dossier's value is the
protocol + thresholds + ItiNera-derived magnitude target.
