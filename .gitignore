pyvenv.cfg
venv
.env
__pycache__/
*.log

# Pi Agent Tasks
docs/devlog
docs/phase-reports
LAUNCH.sh
TASKS

# Flutter
flutter_app/.env
flutter_app/.dart_tool/
flutter_app/build/
flutter_app/.packages
flutter_app/.flutter-plugins
flutter_app/.flutter-plugins-dependencies

# Valhalla routing artifacts (regenerable: 3.6 GB of tiles/PBF/elevation)
# docker-compose rebuilds these on first run from the Geofabrik Egypt extract.
config/valhalla/custom_files/

# Academic Evaluation Data (generated files)
data/evaluation/cache/
data/evaluation/results/

# Test cache files
*.pyc
__pycache__/
*.pyc

# Local Claude settings
.claude/settings.local.json

# Local-only prompt documents (intended to stay untracked — pasted into fresh pi sessions,
# not shared via the repo)
THESIS_PIPELINE_PROMPT.md
design-system/FULL_UI_GENERATION_PROMPT.md

# Local pi-agent orchestration config (agents, chains) — keep on this machine only
.pi/
