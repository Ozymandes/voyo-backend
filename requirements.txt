fastapi>=0.104.0
uvicorn[standard]>=0.24.0
groq>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
overpy>=0.6
python-dotenv>=1.0.0
ratelimit>=2.2.0

# Database & Supabase
supabase>=2.0.0,<2.28.0  # 2.28+ pulls in pyiceberg which requires MSVC to build on Windows
sqlalchemy>=2.0.0

# Additional utilities
urllib3==2.6.2
certifi==2025.11.12

redis>=5.0.0              # Redis caching client
tqdm>=4.66.0              # Progress bars
# concurrent.futures is built into Python 3 — no package needed
